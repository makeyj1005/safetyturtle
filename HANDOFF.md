# 순찰 로봇 프로젝트 — 인수인계 (구버전 이력)

> ⚠️ **현재 기준 인수인계는 `HANDOFF2.md`다 (2026-08-14 전면 재작성).** 이 문서는
> 2026-08-02(VM 중심 구조) 기준의 상세 이력·실측 근거 보관용이다 — 시스템 구조가
> 그 뒤 온보드 Nav2로 바뀌었다. 함정별 상세 재현 과정과 실측 수치를 찾을 때만
> 목차(`grep -nE "^#{1,3} " HANDOFF.md`)로 필요한 절을 열어 볼 것.

최종 업데이트: 2026-08-02 (안전모 감지 실물 검증 완료 — 정지·부저·해제 2사이클 동작)

## 환경

| 항목 | 값 |
|---|---|
| VM (원격 PC) | `ohinseop@ohinseop-virtual-machine`, `192.168.0.52` (bridged, ens33) |
| 로봇 | `rpi@ros18`, MAC **`b8:27:eb:9f:73:ec`** — IP는 재부팅마다 바뀜 (이력: .22 → .66 → .67) |
| 로봇 하드웨어 | TurtleBot3 Burger + Raspberry Pi 3 Model B (SBC와 CSI 카메라가 **같은 한 대**) |
| ROS | 양쪽 Humble, `ROS_DOMAIN_ID=3`, RMW는 **fastrtps**(기본값, cyclonedds 폐기) |
| 카메라 | imx219 CSI, `camera_ros` |

**로봇 IP 찾기:**
```bash
for i in $(seq 1 254); do (ping -c 1 -W 1 192.168.0.$i >/dev/null 2>&1 &); done; sleep 6
ip neigh | grep -i b8:27:eb
```
`b8:27:eb:1a:39:ff`는 **다른 라즈베리파이**다. 끝자리 `9f:73:ec`를 확인할 것.

---

## 현재 상태

### 완료

- **1단계** 센서 확인 — `/scan` 5Hz, `/odom` 20Hz, `/tf`, `/imu`, 카메라 15fps
- **2단계** 라인 추종 — 실험 후 **폐기·삭제**. 카메라 한 대로 바닥(라인)과 정면(안전모·QR)을 동시에 볼 수 없음(imx219 수직화각 49°, 기하학적으로 불가). 목표가 웨이포인트 순찰이라 Nav2로 전환
- **3단계** SLAM 지도 완성
- **4~5단계 일부** Nav2 좌표 이동 검증, 사각형 순찰 웨이포인트 등록
- **5단계** 랜덤 스케줄러 — 실주행 검증 완료 (5회차, 실패·거절 0). 아래 "랜덤 스케줄러" 절
- **지도 개선** patrol_map_v5 (순찰로 다듬음, 원점 동일) — 기본 지도로 채택
- **압력계 판정 모듈** `gauge.py` — 33가지 시험 전부 통과. 아래 "소화기 점검" 절
- **소화기 지점 좌표 등록** 소화기1 (1.105, -0.606) yaw -81.0°
- **압력계 캘리브레이션 등록** 4장 전부 정상/정합 1.00
- **스케줄러 작업 배정 확장** 순찰/점검 중 하나만 배정 — **연동 검증 완료** (순찰 →
  예약 접수 → 그 순번을 점검이 가져감 → 점검 종료 → 다시 순찰)
- **점검 노드** `inspect_node` (2026-08-01) — **실주행 성공.** 이동 → 카메라 정렬 →
  판정 → 부저 → 기록 → 복귀까지 한 번에 돌았다(도착~판정 30초, 정상 판정 변화량 0.53).
  아래 "소화기 점검 노드" 절

### 미검증

- **점검 지점까지의 자율 이동** — 지금까지는 Nav2 없이(또는 `fake_nav2` 로) 확인했다
- 저장한 좌표로 **자율 이동 후 정합 점수 0.9 이상** 나오는지 (점검 기능의 실제 합격 기준)
- QR 내용연한 판정 (인코더·인쇄 PDF 는 준비됨, 부착·판독 미실시)
- 안전모·사람 감지 (계획만, 나중으로 미룸)

---

## 파일 구조

```
~/vibe/ex1/
├── config/patrol_nav2.yaml           Nav2 파라미터 (조정 근거 주석 포함)
├── launch/
│   ├── nav2_patrol.launch.py         순찰용 Nav2 (map_server+AMCL)
│   ├── patrol_auto.launch.py         자동 순찰 (patrol_node + scheduler + inspect_node)
│   ├── inspect_once.launch.py        소화기 점검만 한 번 (시험용, 켜자마자 출발)
│   ├── explore.launch.py             매핑+주행 (cartographer + Nav2 주행부)
│   ├── cartographer_resumable.launch.py   상태 이어서 매핑
│   ├── robot_camera.launch.py        로봇 카메라 (로봇 ~/launch/ 에도 사본)
│   └── vm_camera_relay.launch.py     VM 영상 중계
├── maps/
│   ├── patrol_map_v5.yaml/.pgm       최종 지도 (원점 -1.59, -5.12 / 5cm). patrol_map.* 는 그 이전 버전
│   ├── patrol_waypoints.yaml         순찰 사각형 4점
│   ├── fire_extinguisher_points.yaml 소화기 점검 지점 (활성, 자세 A)
│   ├── fire_extinguisher_points_b.yaml  대안 자세 B (A 가 안 맞으면 이걸로 교체)
│   ├── gauge_calib.yaml              압력계 정상 기준 (ROI·반지름·해상도)
│   ├── gauge_ref_소화기1.png          게이지 기준 패치 (템플릿 정합용)
│   └── state/patrol_state5.pbstream  cartographer 상태 (이어서 매핑용)
├── ros2_ws/src/patrol_core/          ROS2 패키지 (VM에서 빌드)
│   └── patrol_core/
│       ├── cmd_vel_mux.py            /cmd_vel 단독 발행자 (절대 규칙)
│       ├── patrol_node.py            웨이포인트 순찰 (바퀴 수 채우면 스스로 정지)
│       ├── patrol_scheduler.py       랜덤 시각에 순찰/점검 배정 (시각만 안다)
│       ├── inspect_node.py           소화기 점검 (이동→촬영→판정→부저→기록)
│       ├── gauge.py                  압력계 판정 (ROS 의존 없음, 도구와 공유)
│       ├── shot_grab.py              로봇 사진 취득 (ROS 의존 없음, 도구와 공유)
│       └── explore_node.py           frontier 자동 탐사
└── tools/
    ├── save_waypoint.py              좌표 저장 (불확실성·벽간격 검사, --file 로 파일 분리)
    ├── capture_frame.py              카메라 캡처+밝기 분석
    ├── compressed_to_raw.py          영상 중계
    ├── fake_nav2.py                  가짜 Nav2 (로봇 없이 순찰 흐름 확인)
    ├── test_schedule.py              스케줄러 시각 규칙 검사
    ├── patrol_log.py                 순찰 회차 자동 기록 (CSV+사진)
    ├── make_report.py                시험 보고서 PDF 생성 (한글)
    ├── compare_maps.py               지도 두 개 수치 비교
    ├── grab_shot.py                  로봇에 로컬 저장 후 파일 전송 (+ --judge 로 판정)
    ├── live_view.py                  실시간 화면 + 게이지 조준 보조
    ├── gauge_calib.py                압력계 정상 기준 등록
    ├── aim_gauge.py                  teleop 조준 보조 (게이지가 기준 자리에 오게 수치로 안내)
    ├── make_qr.py                    QR 생성 (직접 구현 + zbar 검증)
    └── qr_probe.py                   QR 판독 한계 실측
```

---

## 실행 순서

### 명령 요약 — 어디서 실행하는가

| # | 실행 위치 | 무엇 | 명령 |
|---|---|---|---|
| 0 | VM | 로봇 접속 | `ssh rpi@<IP>` |
| 1 | **로봇** | bringup (모터·라이다·odom) | `ros2 launch turtlebot3_bringup robot.launch.py` |
| 2 | **로봇** | USB 웹캠 스트림 (안전모용) | `python3 ~/launch/webcam_node.py` |
| 3 | VM | **지도 + Nav2 + RViz** (한 번에) | `ros2 launch ~/vibe/ex1/launch/nav2_patrol.launch.py` |
| 4 | VM(RViz 화면) | **2D Pose Estimate** — 명령이 아니라 버튼 | 지도에서 로봇 위치 클릭 → 방향 드래그 |
| 5 | VM | 자동 순찰 + 점검 + 안전모 | `ros2 launch ~/vibe/ex1/launch/patrol_auto.launch.py robot_host:=rpi@<IP>` |
| 6 | VM | 소화기 점검 예약 | `ros2 topic pub --once /inspect/request std_msgs/msg/Bool "{data: true}"` |

처음 한 번(또는 안전모·조명이 바뀌면) **안전모 색 기준 등록** — 2번이 떠 있어야 한다:
```bash
python3 ~/vibe/ex1/tools/helmet_calib.py --grab --name 노란안전모 --select
```

**웹캠을 빼고 점검만 돌릴 때는** 5번에 `camera_index:=0` 을 붙인다. 기본값 1 은 웹캠이
꽂혀 있는 상태 기준이다(꽂으면 CSI 가 0→1 로 밀린다, 함정 9).

**로봇에서 실행하는 건 1·2번뿐이다.** 지도·Nav2·순찰·점검·판정은 전부 VM 이다
(절대 규칙 3: 영상 인식 연산은 VM). CSI 카메라는 손으로 켤 필요가 없다 — 점검할 때
`inspect_node` 가 ssh 로 켰다 끈다. 웹캠은 2번으로 한 번 띄워두면 점검 순번에
`helmet_node` 가 알아서 내리고 올린다(카메라는 항상 한쪽만 켠다).

모든 터미널에서 먼저: `export ROS_DOMAIN_ID=3` (새 터미널엔 안 잡혀 있다)
VM 에서 patrol_core 명령을 쓰기 전: `source ~/vibe/ex1/ros2_ws/install/setup.bash`


### ① 로봇 (터미널 A)
```bash
ssh rpi@<IP>
ros2 launch turtlebot3_bringup robot.launch.py
```
`Run!` 이 나오고 **`Failed to read` 가 없어야** 정상.

토크 확인 (VM 아무 터미널):
```bash
ros2 topic echo /sensor_state --once        # torque: true 여야 함
ros2 service call /motor_power std_srvs/srv/SetBool "{data: true}"   # false 면
```

### ② VM (터미널 B)

순찰만:
```bash
ros2 launch ~/vibe/ex1/launch/nav2_patrol.launch.py
```

매핑도 같이:
```bash
ros2 launch ~/vibe/ex1/launch/explore.launch.py \
  load_state_filename:=/home/ohinseop/vibe/ex1/maps/state/patrol_state5.pbstream
```

**→ RViz 에서 `2D Pose Estimate` 필수.** 좌측 패널의 Navigation / Localization 이 **둘 다 active** 여야 정상.

### ③ VM (터미널 C)

자동 순찰 (2바퀴 → 1분 쉼 → 5분 안의 랜덤 시점에 다시):
```bash
source ~/vibe/ex1/ros2_ws/install/setup.bash && ros2 launch ~/vibe/ex1/launch/patrol_auto.launch.py
```

지금 바로 한 바퀴만 보고 싶을 때:
```bash
source ~/vibe/ex1/ros2_ws/install/setup.bash && ros2 launch ~/vibe/ex1/launch/patrol_auto.launch.py fire_on_start:=true laps:=1
```

스케줄러 없이 손으로 (기존 방식, 무제한 순찰):
```bash
ros2 run patrol_core patrol_node --ros-args -p mode:=loop -p stop_at_corners:=true -p laps_per_run:=0
```

### ④ 소화기 점검을 시키려면 (터미널 D)

`patrol_auto.launch.py` 가 `inspect_node` 도 함께 띄운다(`inspect:=false` 로 뺄 수 있다).
로봇 IP 를 넘겨야 사진을 가져온다:

```bash
ros2 launch ~/vibe/ex1/launch/patrol_auto.launch.py robot_host:=rpi@192.168.0.67
```

점검 예약 (즉시 가지 않는다 — 돌던 순찰을 마치고 **다음 순번**에 간다):
```bash
ros2 topic pub --once /inspect/request std_msgs/msg/Bool "{data: true}"
```

**지금 바로 소화기만 점검 (시험용, 순찰 없음):**
```bash
ros2 launch ~/vibe/ex1/launch/inspect_once.launch.py robot_host:=rpi@192.168.0.67
```
켜자마자 소화기로 가서 판정하고 그 자리에 선다. 좌표·자세를 고치며 반복 확인할 때
쓴다 — `patrol_auto` 로 하면 순번을 기다려야 해서 한 번 보는 데 1~2분이 더 걸린다.
`return_home:=true` 면 실제 운용처럼 순찰 시작지점까지 돌아온다.

⚠️ **`patrol_auto` 와 동시에 띄우지 말 것.** `inspect_node` 가 둘이 되어 각자 Nav2
목표를 보내고 같은 CSV 에 겹쳐 쓴다(실측: 회차 하나를 통째로 버렸다). 노드는 켜진 뒤
15초에 중복을 확인해 ERROR 로 알린다.

확인:
```bash
ros2 topic echo /patrol/schedule    # 다음 순번의 시각과 작업 종류
ros2 topic echo /inspect/status     # 점검 진행 상황
```

### 금지

- **`teleop_keyboard` 동시 실행 금지** — `/cmd_vel` 에 직접 발행해 Nav2 명령을 상쇄시킨다.
  써야 하면 `--ros-args -r /cmd_vel:=/cmd_vel_teleop`
- **기본 `turtlebot3_navigation2` launch 금지** — `inflation_radius: 1.0` 이라 30cm 경로가 곡선이 된다
- **Nav2 테스트 중 `cmd_vel_mux` 금지** — turtlebot3_navigation2 가 `/cmd_vel` 직접 발행해 충돌

---

## 순찰 웨이포인트

`maps/patrol_waypoints.yaml` — **손으로 찍지 않고 지도에서 계산해 넣은 것**

```
좌상 (+0.160, -0.270) → 우상 (+1.310, -0.270)
                              ↓
좌하 (+0.160, -1.220) ← 우하 (+1.310, -1.220)

가로 1.15m × 세로 0.95m, 시계방향
```

벽여유(장애물까지): 좌상 **29cm** / 우상 40cm / 우하 30cm / 좌하 30cm — `patrol_map_v5` 기준.
좌상만 30cm 아래인데 **왼쪽 위 기둥**(20×15cm) 때문이고, 대각선 방향이라 경로가 지나는
왼쪽 변은 기둥에서 25cm 이상 떨어져 있다. burger 반경 10.5cm 를 빼면 18.5cm 여유라
그대로 둔다 — 사각형을 줄이면 순찰 범위가 필요 없이 작아진다. 실주행에서 스치면
좌상만 `x 0.16 → 0.21` 로 5cm 밀면 32cm 가 된다(가로 1.15 → 1.10m).
`tools/compare_maps.py` 로 지도를 비교하고, 위 수치는 미탐사 칸이 아니라
**장애물까지의 거리**로 재야 한다(미탐사 포함으로 재면 좌상이 27cm 로 나온다).

**손으로 찍으면 안 되는 이유:** 위치추정 오차(covariance)가 섞여 y가 20cm씩 오르내리고 경로가 지그재그가 된다. 실제로 두 번 지웠다. 좌표는 `save_waypoint.py` 로 찍되, **직선/사각형은 계산으로 정렬**하는 편이 정확하다.

### 좌표를 바꾸려면

`maps/patrol_waypoints.yaml` 을 직접 고치거나 `tools/save_waypoint.py` 로 추가한다.
`patrol_node` 는 **시작할 때 한 번만** 읽으므로 고친 뒤 노드를 다시 띄워야 적용된다.

- 사각형 4점이어야 하는 건 아니다. **2점 이상이면 동작**한다 (3점=삼각형, 2점=왕복)
- 벽에서 30cm 이상 띄울 것. `save_waypoint.py` 는 벽 간격을 계산해 30cm 미만이면 거부한다
- 파일의 `yaw` 는 **구간 마지막 지점에서만** 쓰인다. 중간 지점에서는 다음 지점을 향하는
  방향을 계산해 쓴다 (`patrol_node.yaw_for`)
- 다른 파일을 쓰려면 `-p waypoint_file:=/경로/다른.yaml`

### 방이 작다

빈공간 6.56m² 중 **30cm 여유 확보 영역은 1.58m²** 뿐. 미탐사가 남은 게 아니라 물리적으로 좁다 (`explore_node` 를 돌려보니 frontier 0개 — 벽 밖 미탐사는 벽으로 완전히 차단되어 갈 수 없음). 넓히려면 막아둔 벽면을 밖으로 옮기고 다시 매핑해야 한다.

---

## 랜덤 스케줄러 · 순찰을 언제 멈추는가

```
patrol_scheduler  →  /patrol/enable=True  →  patrol_node  →  Nav2  →  로봇
(시각만 안다)                                (좌표·바퀴 수를 안다)
```

**정지 기준은 바퀴 수다** (2026-07-30 결정). 시간이 아니라 바퀴 수인 이유: 사각형이
1.15m×0.95m 로 작아 한 바퀴가 짧고, 매 회차 같은 양을 돌아 결과가 일정하다. 시간
기준이면 코너 중간에 끊겨 로봇이 애매한 자세로 선다.

`patrol_node` 가 `laps_per_run`(기본 2) 바퀴를 채우면 **스스로 멈춘다.** 스케줄러는
`True` 만 보내고 `False` 는 보내지 않는다 — 바퀴 수를 아는 건 `patrol_node` 뿐이다.
마지막 바퀴의 끝은 곧 시작지점이라 다음 회차가 제자리에서 이어진다.

### 다음 순찰은 언제 시작하는가 — 기준은 시계가 아니라 직전 순찰의 끝

```
enable=True ── 순찰 (2바퀴) ── done ─┤ 쉼 1분 ├── 랜덤 ──→ 다음 enable=True
                                     └──── 최대 5분 ────┘
다음 시작 = 순찰이 끝난 시각 + 균등난수(rest_min ~ cycle_min)
```

| 파라미터 | 기본 | 뜻 |
|---|---|---|
| `laps` | 1 | 한 회차에 돌 바퀴 수 (0 = 무제한). 2026-08-01 에 2 → 1 |
| `first_laps` | 0 | 첫 회차만 다르게 (0 = 안 씀) |
| `cycle_min` | 2 | 끝난 뒤 다음 순찰까지의 **최대** 시간 (원래 5, 확인하기 좋게 줄임) |
| `rest_min` | 1 | 끝난 직후 **반드시** 쉬는 시간 (이 안에는 절대 시작 안 함) |
| `fire_on_start` | **true** | 켠 직후 첫 회차 시작 (`start_delay` 뒤에) |
| `start_delay` | 10 | 켠 뒤 첫 회차까지 기다리는 시간(초). 0 이면 즉시 |
| `seed` | 0 | 0 이면 매번 다름, 그 외는 재현 가능 |

**켠 뒤 순서를 벌려 놓았다 (2026-08-01 사용자 요청).** 켜자마자 디스커버리·부저
확인·Nav2 목표가 한꺼번에 몰리면, 무선이 느릴 때 첫 목표가 거절되거나 로그가
뒤엉켜 원인을 찾기 어렵다. 그래서 이렇게 벌렸다:

```
 0초   세 노드 시작, DDS 디스커버리
 8초   inspect_node 가 부저(/sound) 확인 결과를 알린다
10초   첫 순찰 회차 시작 (start_delay)
```

`start_delay:=0` 이면 예전처럼 즉시 시작한다.

**켜자마자 한 바퀴 돈다 (2026-08-01 사용자 결정).** `fire_on_start=true` 라
켜면 즉시 1바퀴 → 1분 쉼 → 1~2분 랜덤 → 다시 1바퀴 → ... 모든 회차가 같다.
운용 주기로 되돌리려면 `cycle_min:=5.0`, 켜자마자 도는 게 싫으면 `fire_on_start:=false`.

**점검이 끼어들어도 바퀴가 늘지 않는다.** 점검이 Nav2 목표를 가져가 순찰이 취소되면
`patrol_node` 는 그 회차를 거기서 끝낸다(`done (외부 취소)`). 예전에는 취소를 보고도
다음 목표를 다시 쏴서 점검과 Nav2 를 두고 싸웠다.

**왜 시계 기준을 버렸나 (2026-07-30 변경).** 처음엔 계획서대로 "매 시간 0~50분 사이
랜덤"처럼 시계 경계에서 뽑았다. 주기를 5분으로 줄이자 그 방식이 깨진다 — 순찰 자체가
2~3분이라 시계 기준으로 뽑으면 순찰이 끝난 직후에 걸리거나(쉬는 시간 0) 이미 지나가
그 주기를 건너뛴다. "무조건 돌아오고 1분은 쉰다"를 지키려면 기준점이
**직전 순찰이 끝난 시각**이어야 한다. 최소 간격(`min_gap_min`) 파라미터도 그래서 없앴다:
시작 간격은 자동으로 (순찰 시간 + `rest_min`) 이상이 된다.

**끝난 걸 아는 방법.** `patrol_node` 가 `/patrol/status` 로 `done (...)` 을 발행한다.
스케줄러는 그걸 받은 시점부터 센다. 순찰 중에는 아무것도 발행하지 않으므로 겹치지 않는다.
순찰이 끝나지 않으면 다음 순찰도 잡히지 않는데 이건 의도한 동작이고, `cycle_min` 의
2배를 넘기면 경고가 나온다.

**순찰 길이는 `cycle_min` 과 무관하다.** 기준점이 도착 시각이므로 순찰이 1분이든 6분이든
쉬는 시간은 항상 `rest_min` 이상이고, 겹치는 일도 구조적으로 없다. 로그의 회차 소요
시간은 순찰 빈도(시작~시작 = 소요 + 지연)를 계산할 때만 쓰면 된다.

**검증 (로봇 없이 — `tools/test_schedule.py`, `tools/fake_nav2.py`):**
- 순찰 길이를 1.0 / 2.5 / 6.0분으로 가정한 시뮬레이션에서 쉼과 창이 모두 지켜졌다.
  순찰이 창(5분)보다 긴 6분이어도 깨지지 않는다 — 종료 기준이기 때문이다
- 실제 토픽 왕복 3회차(`rest_min:=0.5 cycle_min:=1.5`): 종료 → 68초 / 75초 / 78초 뒤
  재시작. 모두 30~90초 안

## 소화기 점검 (2026-07-31 작업)

### 무엇을 점검하나 — 압력계 + 내용연한

소화기 일상점검 항목 중 로봇이 할 수 있는 것만 남겼다. 외관 부식·안전핀·호스 균열·
약제 고화는 **로봇이 판정할 수 없다**(사방을 봐야 하고 흔들어봐야 한다). 로봇은
**1차 스크리닝**이고 이상이 있으면 사람이 가서 나머지를 본다.

| 항목 | 판정 | 방법 |
|---|---|---|
| 압력계 | 정상 / 이상 / 판정불가 | 기준 사진과 각도 분포 차이 (`gauge.py`) |
| 존재 | 있음 / 부재 | 게이지 템플릿 정합 점수 + 주변 빨강 비율 |
| 내용연한 | 정상 / 임박 / 만료 / 판독불가 | QR (아직 미실시) |
| 증빙 | 사진 1장 | 원거리 프레임 |

**"판정불가"를 반드시 따로 둔다.** 반사나 흐림으로 바늘을 못 봤을 때 "정상"으로
처리하면 안 된다. 못 봤으면 못 봤다고 하고 사람에게 넘긴다.

### 등록된 값 (실측 확정)

#### 등록 자세 A / B (2026-08-01 재등록)

자세를 다시 등록하면서 후보가 둘 남았다. **A 가 활성**이고, A 로 정합이 0.9 에
못 미치면 B 로 바꿔 시험한다.

| | 좌표 | yaw | 실측 |
|---|---|---|---|
| **A** (활성) | 1.107, −0.555 | **−88.0°** | 정합 **1.00** / dx 0px / 변화량 0.09 — 재등록 직후 제자리 측정 |
| B (대안) | 1.142, −0.576 | −89.2° | 정합 0.87 / dx −68px / 변화량 0.52 — 자율 이동으로 도착한 자세 |

둘은 4.1cm, 1.2° 차이다. A 가 더 좋지만 **그 자세로 자율 이동해서 도달할 수 있는지**는
아직 확인 전이다(그게 다음 할 일 ①). B 로 바꾸려면:

```bash
cp ~/vibe/ex1/maps/fire_extinguisher_points_b.yaml ~/vibe/ex1/maps/fire_extinguisher_points.yaml
```

⚠️ 바꾼 뒤에는 **노드를 다시 띄워야** 적용된다(좌표는 시작할 때 한 번만 읽는다).

```
소화기1   좌표 (1.105, -0.606) yaw -81.0°   벽간격 70cm
          거리 38cm, 게이지 지름 79px, 반사 1.7%
          ROI (729,223,140,140), 게이지 중심 (787,292) 반지름 41px
          해상도 1640x1232 (이 해상도에서만 유효 — ROI 가 픽셀 좌표다)
          검증: 4장 전부 정상 / 정합 1.00
```

파일: `maps/fire_extinguisher_points.yaml`, `maps/gauge_calib.yaml`,
`maps/gauge_ref_소화기1.png`

### 카메라 설정 (점검용)

```bash
# 로봇에서
ros2 launch ~/launch/robot_camera.launch.py camera:=1 width:=1640 height:=1232 fps:=3 jpeg_quality:=85
```
**해상도 상한이 1640x1232 다** — 그 이상은 센서가 전체화면 모드로 바뀌어 CMA 메모리
64MB 를 초과한다. 자세한 근거는 `launch/robot_camera.launch.py` 맨 위 주석.

거리 38~40cm 가 최적이다. 30cm 는 게이지가 크게 보이지만 Nav2 의 `inflation_radius`
0.25m 경계에 걸려 **자율 이동으로 도달하기 어렵다**. 50cm 이상은 게이지가 작아진다.

### 사진은 스트리밍하지 않고 파일로 가져온다

1640x1232 프레임은 120~180KB 다. 무선 DDS 로 보내면:
- `best_effort` : 20초에 **0장** (조각 하나 잃으면 프레임 전체 폐기)
- `reliable` : 20초에 13장 (0.65fps, 최대 5.5초 공백)

로봇 안에서는 정상이므로 **로봇에서 로컬 저장 → scp** 로 가져온다. 점검은 정지 상태에서
몇 장만 필요해 스트리밍이 필요 없다. 조준용 실시간 화면은 640x480 저해상도를 쓴다.

⚠️ `tools/grab_shot.py` 의 scp 호출에 버그가 있다(직접 실행하면 성공, 도구에서는 실패).
지금은 ssh+scp 를 직접 쓰고 있다. 고칠 것.

### 압력계 판정 — 왜 이 방식인가

**바늘의 절대 각도를 매번 찾으려 하면 실패한다.** 다이얼에는 바늘 말고도 어두운 것이
많다(육각 너트와 그림자, 아래쪽 금속, 테두리 반사). 세 번 실패했다:

| 시도 | 결과 |
|---|---|
| Hough 원 검출 + 최소 밝기 각도 | 같은 자세 3장에서 중심이 10px 흔들려 **175° 오판** |
| 반경 기울기 제거 | 거리 배율 0.80/1.35 에서 **145° 오판** (너트 그림자) |
| 넓은 성분 제거 | 다이얼 아래 가느다란 선 때문에 **180° 뒤집힘** |
| **기준 사진과의 차분** ✅ | 고정된 어두운 것이 상쇄되고 움직인 바늘만 남는다 |

실측: 정상(같은 자세/위치 ±10cm/거리 ±35%/조명 ±30%) 변화량 **0.00~0.64**,
바늘 이동(30°~315° 9종) **1.52~3.53**. 임계값 0.9 로 2.4배 여유. 33건 전부 통과.

판정 1회 655ms. 근거 수치는 `gauge.py` 맨 위 주석에 전부 적어뒀다.

## 소화기 점검 노드 (2026-08-01 작업)

```
patrol_scheduler ──/inspect/start──> inspect_node ──> Nav2 이동 ──> 정지
                                          │
                        ssh: 카메라 켜기 → 로컬 저장 → tar 로 회수 → 카메라 끄기
                                          │
                                    gauge.judge()
                                          │
                     정상 → 기록만        이상/부재/판정불가 → 부저(ERROR) + 기록
                                          │
                              /inspect/status 에 done 발행 (스케줄러가 이걸 본다)
```

### 파라미터

| 파라미터 | 기본 | 뜻 |
|---|---|---|
| `robot_host` | `rpi@192.168.0.67` | 사진을 가져올 로봇. **IP 가 바뀌면 이걸 줘야 한다** |
| `shots` | 3 | 지점당 사진 장수 |
| `settle_sec` | 2.0 | 도착 후 흔들림이 가라앉기를 기다리는 시간 |
| `manage_camera` | true | 찍는 순간에만 로봇 카메라를 켰다 끈다 |
| `max_attempts` | 3 | 판정불가일 때 자세를 틀어 다시 시도 (0°, +8°, -8°) |
| `sound` | true | 판정을 소리로 알린다 (아래 "부저" 참고) |
| `sound_ok_value` / `sound_ok_repeat` | 1 / 4 | 정상음 번호와 반복 횟수 |
| `sound_bad_value` / `sound_bad_repeat` | 2 / 1 | 이상음 번호와 반복 횟수 |
| `sound_gap_sec` | 0.25 | 반복 사이 간격(초) |
| `return_to_start` | true | 끝나면 순찰 시작지점으로 복귀한 뒤 `done` 발행 |
| `max_attempts` | 3 | 자세가 어긋나면 사진으로 각도를 계산해 다시 시도하는 횟수 |
| `retry_yaw_deg` | 8 | 게이지를 아예 못 찾아 계산이 안 될 때 훑는 각도 |
| `photo_dir` | (빈값) | 로봇 없이 시험: 이 폴더 사진으로 판정한다 |

### 도착한 뒤 카메라로 자세를 맞춘다 (2026-08-01, 실주행 성공)

**지도 각도를 믿지 않는다.** 저장된 yaw 는 지도 좌표라 2D Pose Estimate 가 몇 도
틀어지면 실제 자세가 그만큼 틀어진다. 실제로 그것 때문에 소화기를 1m 밖에서 찍거나
화면 밖으로 놓쳤다. 그래서 도착 후 **카메라를 보며 제자리 회전**으로 맞춘다:

```
Nav2 로 좌표까지 이동 (위치만 지도에 의존)
  → 카메라를 켜둔다 (한 장 7~9초. 껐다 켜면 30~40초라 되먹임을 못 돈다)
  → 사진 1장 → 화면 전체에서 게이지를 찾는다 → 기준 자리에서 몇 px 벗어났나
  → /spin 으로 그만큼 돌린다 (상대 회전이라 지도 오차와 무관)
  → 120px 안에 들면 판정용 3장 촬영 → 카메라 끈다
```

`/spin` 이 핵심이다. Nav2 목표 yaw 는 지도 좌표지만 `/spin` 은 "지금부터 N도"다.
회전은 Nav2 가 하므로 `/cmd_vel` 절대 규칙도 지킨다.

**환산값(1도당 픽셀)은 돌려본 결과로 직접 잰다.** 카메라가 후면이라 방향을 머리로
따지면 틀린다(두 번 틀렸다). 실측 **1도당 +101px**(명령 기준) — 기하학적 26px/도의
4배인데, `/spin` 이 명령보다 많이 도는 것으로 보인다. 제어에는 명령 기준값이 맞다.

| 파라미터 | 기본 | 근거 |
|---|---|---|
| `align_tol_px` | 120 | 판정 문턱과 같게. 더 조이면 잡음(±100~200px)을 쫓아 진동한다 |
| `align_min_step_deg` | 1.0 | 허용오차(120px=1.2°)보다 작아야 한다. 2°로 뒀더니 교착에 빠졌다 |
| `align_settle_sec` | 1.5 | 회전 직후엔 흔들려서 위치가 튄다 |
| `align_scan_deg` | 15 | 게이지가 화면에 없으면 +15/-30/+45/-60° 로 훑는다 |

**실주행 결과 (2026-08-01 18:47)** — 처음으로 끝까지 성공:
```
dx +232px → -4.4° 회전 → dx -64px (맞음) → 사진 3장 → 정상(변화량 0.60) → 긴 소리
```
정렬 1회, 도착부터 판정까지 30초.

#### 자세 어긋남과 변화량의 관계 (실측)

| 어긋남 | 변화량 | 문턱 0.9 까지 여유 |
|---|---|---|
| 0px | 0.09~0.20 | 0.70 |
| 64px | 0.60 | 0.30 |
| 93px | 0.49 | 0.41 |
| 166~182px | 0.58~0.76 | **0.14** — 여기선 헛경보 위험 |

그래서 판정 문턱(`max_offset_px`)을 180px 대로 풀 수 없다. 정렬로 120px 안에
넣는 편이 맞다.

### 결정한 것과 그 이유

**끝나면 순찰 시작지점으로 돌아간 뒤 `done` 을 발행한다** (`return_to_start`, 2026-08-01).
스케줄러가 `done` 시각부터 쉼을 세므로, 쉬는 시간을 시작지점에서 보내고 다음 순찰이
이동 없이 바로 시작된다. 안 그러면 소화기 앞에서 쉬다가 순찰이 시작된 뒤에야
시작지점으로 이동해서 그 이동 시간이 순찰 회차에 붙는다. 복귀 방향은 첫 웨이포인트에서
두 번째 웨이포인트를 향하는 각도로 맞춰, 출발할 때 제자리 회전이 없게 한다.

**자세가 어긋나면 판정하지 않고 사진으로 각도를 고쳐 다시 본다** (2026-08-01).
`gauge.judge` 가 게이지가 기준 자리에서 몇 px 벗어났는지(`offset`)와 몇 도 돌려야
하는지(`yaw_fix_deg`)를 준다. `inspect_node` 는 그만큼 돌린 yaw 로 목표를 다시 보낸다.
지도·AMCL 이 아니라 카메라로 닫는 되먹임이라 위치추정 오차와 무관하다. 자세한 근거는
아래 "함정 16".

**여러 장은 나쁜 쪽으로 합친다.** 한 장이라도 이상/부재/판정불가면 그 지점을 그렇게
기록한다(우선순위 부재 > 이상 > 판정불가 > 정상). 반사·흔들림으로 한 장만 흐렸어도
사람이 한 번 더 보는 편이 낫다 — 놓치는 쪽이 더 나쁘다. CSV 의 `per_shot` 열에 장별
판정을 모두 남기므로 몇 대 몇이었는지 볼 수 있다.

**판정불가면 자세를 틀어 다시 본다.** 정합이 낮은 원인은 대개 자세(게이지가 ROI 밖).
제자리 회전을 직접 시키려면 `/cmd_vel` 을 발행해야 하는데 그건 절대 규칙 위반이라,
**같은 좌표에 yaw 만 다른 목표를 Nav2 에 다시 보낸다**(회전은 Nav2 가 한다).
이상·부재는 다시 봐도 같으므로 재시도하지 않는다.

**yaw 는 저장된 값을 그대로 쓴다.** CSI 카메라가 로봇 **후면**에 달려 있어서,
`save_waypoint.py` 가 저장한 로봇 heading 이 곧 "그 사진이 나오던 자세"다.
`patrol_node` 처럼 "다음 지점을 향하도록" 방향을 계산하면 안 된다.

**부저 — 정상은 길게, 이상은 짧게** (2026-08-01 사용자 결정, 실기 확인)

| 판정 | 음 | 횟수 | 들리는 느낌 |
|---|---|---|---|
| 정상 | **1번** | 4회 (0.25초 간격) | 길게 |
| 이상·부재·판정불가 | **2번** | 1회 | 짧게 |

정상에도 울리는 이유: 소리가 아예 안 나면 "정상이라 조용한 건지, 노드가 죽은 건지"를
현장에서 구분할 수 없다. 시연에서 **로봇이 압력계를 정상으로 인식했다**는 걸 소리
길이만으로 알 수 있게 한 것이기도 하다.

`/sound` 서비스에는 길이 인자가 없어(값 0~5 의 고정 음만) **반복으로 길이를 만든다.**
값·횟수·간격은 전부 파라미터다. `sound:=false` 로 전부 끌 수 있다.

⚠️ **값 2 는 OpenCR 이 저전압일 때 스스로 내는 음과 같다.** 배터리가 11V 아래로
떨어지면 같은 소리가 나므로, 경보가 잦으면 판정을 의심하기 전에 전압부터 볼 것.

**끝나면 무슨 일이 있어도 `done` 을 발행한다.** 예외가 나도 발행한다 — 안 하면
스케줄러가 다음 순번을 잡지 못해 순찰까지 멈춘다(함정 8과 같은 상황).

### 출력

```
logs/inspect_<월일_시각>.csv     지점별 한 줄 (판정, 변화량, 각도, 정합, 반사, 빨강, nav, 사유)
logs/inspect_<월일_시각>/        증빙 사진 (판정을 그려 넣은 것) + raw/ (원본 프레임)
```

**판정이 끝나면 사람이 읽을 요약을 한 상자로 찍는다** (2026-08-01 추가). 로그가 길어
결과가 묻히지 않게 하려는 것이다. 정상은 INFO(흰색), 나머지는 ERROR(빨강)로 나온다:

```
──────────────────────────────────────────────
  [정상]  소화기1 압력계 정상 — 압력이 기준과 같다
  근거: 변화량 0.53 (문턱 0.90) / 정합 0.98 / 사진 정상/정상/정상
  증빙: logs/inspect_0801_.../소화기1_a1_정상.png
──────────────────────────────────────────────
```

`/inspect/status` 토픽에도 `소화기1 압력계 정상` 형태로 나간다(다른 노드·화면에서 바로 쓰라고).

### 실측 (2026-08-01, 로봇 실기)

| 항목 | 값 |
|---|---|
| 사진 3장 취득 (카메라 켜고 끄기 포함) | 27~43초 |
| 로봇 안 저장 자체 | 3장 5~6초 |
| 정상 판정 | 변화량 0.08, 정합 1.00 |
| 이상 판정 (바늘 150° 합성) | 변화량 2.21, 검출 각도 150° |
| 부재 판정 (로봇이 딴 데를 볼 때) | 정합 0.35~0.42, 빨강 0.0% |

## Nav2 파라미터 조정 내역

`config/patrol_nav2.yaml` — **원본은 `param/humble/burger.yaml`** (상위 폴더의 `burger.yaml` 은 Jazzy 이후용이라 Humble 에서 plugin 을 못 찾고 planner 가 FATAL 종료한다)

| 항목 | 원래 | 변경 | 이유 |
|---|---|---|---|
| `inflation_radius` (전역) | 1.0 | **0.25** | 벽 30cm 경로가 고비용 구역 안이라 곡선이 됐다 |
| `inflation_radius` (지역) | 0.55 | 0.25 | 위와 동일 |
| `xy_goal_tolerance` | 0.25 | **0.10** | 웨이포인트 간격의 절반이라 지점을 대충 지났다 |
| `yaw_goal_tolerance` | 0.25 | **0.05** | 0.20(11.5°)이면 점검에서 게이지를 놓친다(아래) |
| `max_vel_x` / `max_speed_xy` | 0.22 | **0.15** | 벽까지 여유 20cm(30cm − 로봇반경 10cm) |
| `sim_time` | 1.5 | **1.0** | 좁은 코너를 조여서 돌도록 |
| `use_sim_time` (local_costmap) | True | **False** | 원본 오류 |

---

## 자주 겪은 함정

### 1. 로봇에선 토픽이 보이는데 VM 에선 안 보임
```bash
ros2 daemon stop && rm -rf /dev/shm/fastrtps_* && ros2 daemon start
```
VM 재부팅 후에도 필요했다. `ros2 node list` 가 비거나 `/odom` 만 안 보이는 식으로 나타난다.

⚠️ **`ros2 topic list` 자체가 거짓말을 할 수 있다.** 데몬이 낡으면 토픽이 하나도 없다고
나온다(2026-08-01 실측: 목록에 `/rosout` 만 남았는데 실제로는 전부 정상이었다).
의심되면 데몬을 거치지 않고 확인할 것:

```bash
ros2 topic list --no-daemon
```

### 2. `Navigation: inactive` / `navigate_to_pose action server is not available`
파라미터 파일 버전 불일치. `planner_server` 가 FATAL 종료하며 Navigation 그룹 전체가 죽는다.
`param/humble/burger.yaml` 기반인지 확인할 것.

### 3. `Nav2 가 목표를 거절했다` 반복 / `Invalid frame ID "odom"`
`turtlebot3_node` 가 죽었거나 **Nav2 를 켤 때 bringup 이 안 떠 있던** 상태다.
Nav2 잘못이 아니다. 이런 로그가 계속 나온다:

```
local_costmap: Timed out waiting for transform from base_link to odom ...
tf error: Invalid frame ID "odom" passed to canTransform argument target_frame
```

확인: `/tf` 에 `odom -> base_footprint` 가 오는지 본다. 없으면 bringup 을 먼저 띄우고
**Nav2 를 다시 시작**한 뒤 2D Pose Estimate 를 새로 찍는다.
(2026-08-01: 라이다를 고치느라 bringup 을 여러 번 껐다 켜는 사이에 이걸 겪었다)

### 4. 로봇이 안 움직임
진단 순서: ① `/cmd_vel` 에 값이 나가는지 ② `torque` 상태 ③ `/odom` 이 변하는지 (e-11 수준이면 바퀴가 안 돈 것)

### 5. `There is no status packet!` → `stack smashing detected` → exit -6
모터 전원 문제. OpenCR 은 USB 만으로도 논리회로가 켜져서 `/dev/ttyACM0` 은 보이지만 **모터는 배터리 12V 가 따로 필요**하다. 배터리 연결 / OpenCR 전원 스위치 / 모터 케이블 확인.

### 6. 배터리
3셀 리튬폴리머. 하루에 12.11V(89%) → 10.99V(27%) 소모. **11V 아래면 OpenCR 이 "삐" 경고음을 내고 `turtlebot3_node` 가 죽는다.**
→ **개발·테스트는 SMPS 어댑터로 하는 게 낫다.** 순찰 영역이 1.15m×0.95m 로 작아 전선이 문제되지 않는다.

### 7. 스케줄러가 발행했는데 순찰이 시작되지 않음

`/patrol/enable` QoS 가 VOLATILE 이라 **구독자가 없을 때 발행하면 메시지가 사라진다.**
launch 로 두 노드를 함께 띄우면 스케줄러가 먼저 올라와 첫 트리거를 잃는다
(실측: 0.08초 차이로 놓쳤다). 그래서 스케줄러는 구독자가 생길 때까지
`subscriber_wait_sec`(기본 30초) 동안 기다렸다 발행한다.
TRANSIENT_LOCAL 로 바꾸면 해결되지만, `patrol_node` 재시작 때 지난 `True` 가 곧바로
배달돼 로봇이 예고 없이 움직이므로 택하지 않았다.

### 8. 순찰이 끝났는데 다음 순찰이 안 잡힘

스케줄러는 `/patrol/status` 의 `done ...` 을 보고 다음 시각을 뽑는다. 그 문자열이
안 오면(노드 사망, 목표 거절 반복으로 회차가 끝나지 않음) 다음 순찰이 안 잡힌다.
`cycle_min` 의 2배가 지나면 경고가 나온다. `ros2 topic echo /patrol/status` 로 확인할 것.

`ros2 topic echo /patrol/schedule` 은 다음 예정 시각을 보여준다.

(참고 — 시계 기준으로 뽑던 옛 방식에서는 같은 시간대에 두 번 시작되는 버그가 있었다.
직전 발행이 있던 주기부터 다시 찾으면 창이 넓어 두 번 뽑힌다. 지금은 종료 기준이라
구조적으로 발생하지 않는다.)

### 9. 카메라·라이다 관련 함정 (2026-07-31)

**camera 인덱스가 밀린다.** USB 웹캠을 달자 libcamera 가 카메라를 두 대로 보게 되어
`camera:=0` 이 웹캠, `camera:=1` 이 CSI 가 됐다. 증상: `unsupported pixel format "RGB888"`
로 exit -6. USB 를 빼면 다시 0 으로 돌아온다.

**USB 카메라를 라이다 스캔 평면에 두면 안 된다.** 처음 장착 위치에서 라이다가
자기 카메라를 장애물로 봤다 — 9cm(센서 최소거리 12cm 미만 = 불가능한 값)에 96° 폭의
가짜 벽이 생겼다. `inflation_radius` 0.25m 때문에 로봇이 갇힌 것으로 판단하고,
AMCL 도 지도에 없는 벽을 따라다녀 위치추정이 나빠진다. **카메라를 스캔 평면 위로
옮겨 해결**했다(옮긴 뒤 12cm 미만 0빔, 25cm 미만 0빔).

**진단법**: `/scan` 에서 12cm 미만 값이 있으면 로봇에 붙은 물체다. 제자리 회전을
시켜보면 판별된다 — 로봇과 같이 돌면 자기 가림, 제자리에 남으면 실제 벽.

### 10. AMCL 은 로봇이 움직여야 갱신한다 (2026-07-31)

`update_min_a: 0.2`(11.5°), `update_min_d: 0.25m`. 이보다 작게 움직이면 **AMCL 이
갱신을 안 하고, `/amcl_pose` 는 옛 값을 그대로 준다.** 8cm 물러난 뒤 좌표를 저장하려
했더니 물러나기 전 좌표가 저장될 상황이었다.

**좌표 저장 전에 제자리에서 20° 씩 두세 번 회전할 것.** 두 가지가 동시에 해결된다:
갱신이 일어나고, 회전이 방향 추정을 수렴시킨다. 실측 효과:

| | 회전 전 | 회전 후 |
|---|---|---|
| x 불확실성 | 0.077 | **0.033** |
| y 불확실성 | 0.103 | **0.027** |
| yaw 불확실성 | 0.244 (±28°) | **0.077 (±16°)** |

### 11. 자동 ROI 가 라벨의 "OK" 로고를 게이지로 잡는다 (2026-07-31)

로고 원이 93px, 게이지가 80px 로 크기 차이가 14% 뿐이라 크기로는 구분되지 않는다.
`gauge_calib.py` 의 자동 ROI 가 실제로 로고를 잡아 엉뚱한 기준을 등록했다.
**`--roi x,y,w,h` 로 직접 지정하거나 `--select` 로 드래그할 것.** 등록 후
`검출: 중심(...)` 좌표가 게이지 위치와 맞는지 눈으로 확인해야 한다.

### 12. 로봇이 안 움직이고 Nav2 가 목표를 거절할 때 — 진단 순서 (2026-07-31 실측)

이 문제로 한 시간을 썼다. 순서대로 확인하면 빠르다.

```
① ping 로봇  → RTT 가 1000ms 대면 무선 포화다 (정상 5~30ms)
   원인 1위: 카메라 스트림. 1640x1232 reliable 은 프레임 177KB 를 재전송까지 하며
             대역을 다 먹는다. /scan·/odom 같은 작은 메시지가 뒤로 밀린다
   → 순찰·주행 중에는 카메라를 반드시 끌 것

② /odom, /tf 수신 확인 → 0Hz 면 odom 프레임이 없어 Nav2 가 모든 목표를 거절한다
   /battery_state·/imu 는 오는데 /odom·/tf 만 0 이면 turtlebot3_node 가 죽어가는 중

③ 로봇에서 pgrep 로 bringup 확인 → 없으면 다시 띄운다
   ⚠️ pgrep -f 는 자기 명령줄까지 세므로 개수가 +1 로 보인다. ps 로 확인할 것

④ 그래도 느리면 로봇 안에서 재본다 (로컬이 정상이면 무선, 로컬도 느리면 로봇)
   ⚠️ 측정 스크립트가 병목일 수 있다. 메시지의 header.stamp 간격을 보면
      유실인지 측정 오류인지 구분된다 (간격 0.2s = 유실 없음)

⑤ 스로틀링 확인: vcgencmd get_throttled (0x0 = 정상), 저전압 경고는 dmesg
```

실측 정상값: `/odom` 16~20Hz, `/scan` 4.3~5Hz, ping 7~64ms(평균 30ms), 손실 0%,
Wi-Fi 신호 97% / 130Mbit/s, 로봇 load 1.3, CPU turtlebot3_ros 12%.

### 13. 매핑 시 (다시 매핑할 일이 생기면)
- **모서리에서 시작** — 평평한 벽 하나만으로는 벽을 따라 얼마나 이동했는지 알 수 없어 위치가 안 고정된다
- **손으로 들지 말 것** — 오도메트리가 어긋나 벽이 이중으로 그려진다 (실제로 겪고 `.pbstream` 으로 되돌렸다)
- 진행 중 저장 (cartographer 끄지 않고 다른 터미널에서):
  ```bash
  ros2 service call /write_state cartographer_ros_msgs/srv/WriteState \
    "{filename: '/home/ohinseop/vibe/ex1/maps/state/새이름.pbstream', include_unfinished_submaps: true}"
  ```
- `turtlebot3_cartographer` 원본 launch 는 `load_state_filename` 을 **조용히 무시**한다. `cartographer_resumable.launch.py` 를 쓸 것 (`-load_frozen_state false` 명시 필요 — 기본 true 는 위치추정만 함)

### 14. 카메라를 껐는데 안 꺼졌다 — `ros2 launch` 를 죽여도 자식이 산다 (2026-08-01)

`ros2 launch robot_camera.launch.py` 를 백그라운드로 띄우고 그 PID 에 `kill -INT`
→ 안 죽어서 `kill -9` 했더니, **`camera_node` 자식이 PPID=1 로 고아가 되어 계속
스트리밍**했다. 사진은 잘 찍혔으니 아무도 눈치채지 못한다. 그 상태로 순찰을 돌면
무선이 포화돼 Nav2 가 목표를 거절한다(함정 12 와 같은 증상, 원인은 우리가 만든 것).

`shot_grab.py` 는 이제 launch 가 아니라 **`camera_node` 를 이름으로** 확인하고
없어질 때까지 SIGINT → SIGKILL 하며, 남으면 `camera STILL RUNNING` 을 보고한다
(`inspect_node` 가 ERROR 로 올린다).

확인·수동 정리:
```bash
ssh rpi@<IP> 'ps -eo pid,ppid,cmd | grep [c]amera_node'
ssh rpi@<IP> 'pkill -f camera_node'
```

⚠️ `pkill -f` 를 쓸 때는 **자기 명령줄까지 죽인다**. 원격 스크립트 안에서는
`'[c]amera_node'` 처럼 써야 한다(이 실수로 로컬 셸을 세 번 죽였다).

### 16. 점검 사진에 게이지가 안 잡힌다 — 각도 허용오차 때문 (2026-08-01 실측)

자율 이동 후 판정이 `부재`(정합 0.39)로 나왔다. 판정 로직이 아니라 **선 방향**이 문제였다.

| | 목표 | 실제 | 차이 |
|---|---|---|---|
| x | 1.105 | 1.102 | −3mm |
| y | −0.606 | −0.601 | +5mm |
| **yaw** | **−81.0°** | **−91.0°** | **−10.0°** |

위치는 5mm 로 훌륭한데 각도가 10° 틀어졌다. 원인은 `yaw_goal_tolerance: 0.20`
(= 11.46°) — **오차 10°가 허용치 안이라 Nav2 가 성공으로 처리하고 멈춘다.**
38cm 거리에서 10°는 화면 약 260px 이동이고, 게이지 지름이 79px 이니 ROI 밖으로 나간다.
사진을 보면 소화기가 프레임 오른쪽 끝에 걸려 있고 게이지는 안 보인다.

같이 드러난 것: **재시도 각도(±8°)가 허용오차(±11.5°)보다 작으면 의미가 없다.**
Nav2 가 그 범위 안 아무 데나 멈출 수 있어 재시도해도 자세가 안 바뀐다.

#### 회전 방향(부호)은 추측하지 말고 재라 — 두 번 틀렸다

기준 사진과 오늘 사진에서 같은 패치를 화면 전체에서 찾으면 **dx +492px** 이 나온다.
여기서 "yaw 를 어느 쪽으로 돌려야 하나"를 추론하려면 *기준 사진이 어느 yaw 에서
찍혔는지*를 알아야 하는데 그 기록이 없다. 저장된 −81° 라고 가정하고 부호를 정했다가
실제 주행에서 **로봇이 대상에서 멀어지는 쪽으로 돌았다**(사용자가 보고 잡아냈다).

그래서 부호도 크기도 **로봇의 반응에서 직접 잰다**:

```
k = Δ화면픽셀 / Δ명령각도      (부호가 여기 같이 들어 있다)
다음 보정 = -dx / k * 0.9      (한 번에 최대 30°)
```

한 번 돌려보면 k 가 나오고, 그 뒤로는 노드가 사는 동안 계속 쓴다. 아직 못 쟀을
때만 기본 환산값(26.4px/도 = 화각 62.2°/1640px)으로 **절반만**, 최대 12°까지
돌려본다 — 그 시도가 곧 측정용 탐침이 된다.

시뮬레이션(실제 k 를 20~60px/도, 부호 양쪽, 300회씩): 계통오차 0°면 96~100%가
1회 시도로, 10°면 85%가 평균 2.9회로 판정에 성공한다. **20~30° 계통오차에서는
60~75% 로 떨어진다** — 보정 루프는 작은 오차용이지 큰 miscalibration 용이 아니다.

#### 고친 방식 — 세 가지를 같이

1. `yaw_goal_tolerance` **0.20 → 0.05**(2.9°). 목표에 실제로 도달하게 한다
2. `gauge.py` 의 `search_pad` **420 → 700**. 어긋난 게이지를 일단 찾을 수 있어야 한다
   (오늘 492px 은 420 밖이라 "부재"로 오판됐다)
3. **자세가 어긋나면 판정하지 않는다** (`max_offset_px` 150). 찾은 게이지가 기준
   자리에서 그 이상 벗어나면 판정불가로 두고, `yaw_fix_deg` 를 계산해 그만큼 돌린
   목표를 다시 보낸다 — `inspect_node` 의 재시도가 이 값을 쓴다

**3번이 특히 중요하다.** 넓게 찾기만 하고 그대로 판정했더니 바늘이 그대로인데도
변화량 2.16 이 나와 **"이상"으로 오판**했다. 보는 각도가 달라지면 밝기 분포가
달라지기 때문이다 — 이 판정법은 "같은 자세"가 전제다. 헛경보는 놓치는 것만큼 나쁘다.

검증: 오늘 사진으로 계산한 보정각이 **+9.8°** 로 실제 AMCL 오차 10° 와 일치했다.
실측 관계식을 넣은 시뮬레이션(400회)에서 판정 성공률 64% → **100%**(평균 1.0회 시도).

#### ⚠️ 남은 진짜 문제 — 등록된 자세가 기준 사진과 다르다

자율 이동으로 저장된 좌표에 정확히 갔는데도 게이지가 480px 밀려 있다는 건,
**`fire_extinguisher_points.yaml` 의 yaw(−81°)가 기준 사진을 찍은 자세와 다르다**는
뜻이다(픽셀로 환산하면 10~19°). 보정 루프로 매 회차 이걸 따라잡는 건 낭비고
성공률도 떨어진다. 제대로 된 해법은 **자세를 다시 등록하는 것**:

1. `tools/aim_gauge.py` 를 켜고 teleop 으로 몰아 게이지를 기준 자리에 맞춘다.
   몇 초마다 사진을 받아 `dx / dy / 배율` 을 보여주고, 맞으면 "저장할 준비가 됐다"가 뜬다.
   회전 방향은 도구가 /odom 을 보며 스스로 배운다(1도에 몇 px 인지 재서 "왼쪽 3.2도"로 안내)
2. 그 자리에서 `save_waypoint.py --file fire_extinguisher_points.yaml` 로 좌표를 덮어쓴다
   (저장 전 제자리 회전 두세 번 — 함정 10)
3. 필요하면 `gauge_calib.py` 로 기준 패치도 그 자세에서 다시 뜬다

**조준 중에는 카메라를 계속 켜둔다.** 매번 켰다 끄면 한 장에 35~43초라 조준이 안 된다.
켜둔 채로는 **한 장에 7초**(실측). 도구가 끝날 때 자동으로 끈다 — 주행 전에 카메라가
남아있으면 안 되기 때문이다(함정 14).

이렇게 하면 계통오차가 0 에 가까워져 첫 시도에 판정된다(시뮬레이션 96~100%).

판정 문턱(`max_offset_px` 150)도 근거가 약하다. 오늘 두 점(0px→변화량 0.08,
481px→2.16)에서 보수적으로 잡은 값이라, **알려진 각도(1°, 2°, 3°)만큼 일부러 틀어
찍어 변화량이 언제부터 커지는지 재서** 정하는 게 맞다.

### 19. 자세가 조금만 어긋나도 헛경보가 났다 — 정렬 탐색으로 해결 (2026-08-01)

바늘은 멀쩡한데 `이상` 이 나왔다. 원인은 판정이 자세에 너무 예민했던 것:

| 화면 어긋남 | 변화량 | 판정 |
|---|---|---|
| 0px | 0.09~0.12 | 정상 |
| **93px** | **1.25~1.28** | 이상 ← 헛경보 |

93px 은 각도로 1.6~3.5° 밖에 안 된다. Nav2 의 각도 허용오차(2.9°)만으로도 그만큼
생기므로 **재등록만으로는 재발한다.** 템플릿 정합이 몇 px 빗나가면 고리가 다이얼의
다른 곳을 훑어 분포가 통째로 달라지기 때문이다.

**해결: 비교 전에 중심·반지름을 조금씩 흔들어 보고 가장 잘 맞는 자리의 값을 쓴다**
(`align_px` ±4px / step 2, `align_scales` 0.96·1.0·1.04 = 75가지).
바늘이 진짜 움직였으면 어떻게 맞춰도 값이 안 내려가므로 헛경보만 걷힌다.

| | 탐색 없음 | **±4px/2 배율3** | ±8px/2 배율5 |
|---|---|---|---|
| 93px 어긋남(정상) | 1.25 ❌ | **0.49** ✔ | 0.49 ✔ |
| 바늘 240° 이동 | 2.32 | **2.32** | 1.28 ⚠ 깎임 |
| 판정 1회 | 1.8초 | **1.8초** | 2.5초 |

**넓게 흔들수록 진짜 이상까지 깎아먹는다** — ±8px 로 넓히자 바늘 240° 가 2.32 → 1.28
로 떨어졌다. ±4px 가 헛경보를 없애면서 진짜 이상을 지키는 지점이다.

검증 8건 전부 통과: 헛경보 2건 → 정상(0.12/0.49), 바늘 이동 4종 → 이상(1.60~2.43),
어긋남+바늘 2건 → 이상(2.54/2.68). 정상 최대 0.53 vs 이상 최소 1.60 으로 3배 여유.

### 18. 같은 노드를 두 번 띄우면 회차가 통째로 망가진다 (2026-08-01 실측)

시험용으로 띄워둔 `inspect_node` 하나가 1시간 넘게 살아 있었다. 스케줄러가 점검을
시작시키자 **두 노드가 모두 반응해** 각자 Nav2 목표를 보내고(서로 취소시키고) 같은
CSV 에 겹쳐 썼다. 증상:

- 결과 CSV 가 깨진다(두 프로세스가 같은 파일에 동시 쓰기) — 이걸로 알아챘다
- 스케줄러가 `점검 종료` 를 두 번 잡는다
- 복귀가 0.1초 만에 "완료" (엉뚱한 위치에서 목표를 받아 이미 도착 판정)
- 사진에 소화기가 아예 없다(로봇이 딴 데를 본다)

원인은 `ros2 run` 을 백그라운드로 띄우고 **껍데기 PID 만 kill** 해서 자식 노드가
살아남은 것이다(카메라 고아 프로세스와 같은 패턴 — 함정 17).

```bash
ps -eo pid,etime,cmd | grep [i]nspect_node     # 오래된 것을 찾아
kill -9 <PID>
```

`inspect_node` 는 이제 켜진 뒤 15초에 같은 이름 노드 수를 세어 둘 이상이면
ERROR 로 알린다.

### 17. 매달린 카메라가 장치를 붙들면 새 카메라가 죽는다 (2026-08-01 실측)

증상: 카메라를 띄웠는데 프레임이 0장. `/tmp/*_cam.log` 에
`Pipeline handler in use by another process` → `failed to acquire camera` → exit -6.

원인: **앞서 뜬 camera_node 가 매달린 채(hung) 살아있다.** 실측한 놈은 PPID=1 고아에
상태 `Sl`, wchan `futex_wait_queue_me` 로 25분째 `/dev/media0` 만 붙들고 프레임은
한 장도 안 냈다. **SIGTERM 으로는 안 죽는다 — SIGKILL 이 필요하다.**

```bash
ssh rpi@<IP> 'ps -eo pid,stat,etime,cmd | grep [c]amera'
ssh rpi@<IP> 'pkill -9 -f camera_node'
```

이렇게 남는 경로: 도구가 끝나며 보내는 "카메라 꺼라" ssh 가 **무선 문제로 조용히
실패**하면 그대로 남는다. `aim_gauge.py` 는 이제 ① 켜기 전에 먼저 지우고 ② 켠 뒤
한 장 받아 확인하고 ③ 두 번 연속 실패하면 다시 띄우고 ④ 끌 때 3번까지 확인한다.

### 16-2. ssh 연결을 재사용하면 조준이 빨라진다

무선이 나쁠 때 ssh handshake 가 여러 번 왕복해 사진 한 장에 30초가 넘었다.
`ControlMaster=auto` + `ControlPersist` 로 통로를 재사용하면 접속 부담이
9초 → 5.4초로 줄고, 카메라를 켜둔 상태에서 **한 장 평균 8.4초**(6.4~11.5)가 된다.
`patrol_core/shot_grab.py` 의 `SSH_OPTS` 에 들어 있다 — 점검 노드도 같이 쓴다.

### 15. `/sound` 를 찾는 데 30초가 걸릴 수 있다 (2026-08-01)

무선 DDS 는 **원격 서비스 발견이 느리고 편차가 크다** — 실측 11.5초, 어떤 때는
30초 이상. `wait_for_service(2초)` 로 잡으면 부저가 멀쩡한데 "없다"고 넘어간다.
`ros2 service call /sound ...` 이 응답 없이 멈춘 것처럼 보이는 것도 같은 이유다
(`waiting for service to become available...` 을 출력 중인데 파이프로 넘기면
버퍼링돼 안 보인다 — `stdbuf -oL` 로 확인할 것).

`inspect_node` 는 시작 후 20초마다 최대 5번 부저를 확인해 `부저(/sound) 확인됨` /
경고를 남기고, 실제 호출은 `sound_wait_sec`(기본 15초)까지 기다린다.
부저 자체는 정상이다: `success=True, 'Succeeded to write data'` (로봇·VM 양쪽 확인).


---

## 다음 할 일

### ① 안전모 감지 — **실물 검증 완료** (2026-08-02)

**목표:** 일상 순찰 중 사람이 보이면 **안전모 착용 여부를 판정**하고, 미착용이면
**정지 + 경보음**. 해제되면 순찰을 이어간다.

#### 카메라는 항상 한쪽만 켠다 (2026-08-01 사용자 결정)

```
일반 순찰 : USB 웹캠(로봇 webcam_node, 640x480 3fps) ─무선─> VM helmet_node
                                                              ↓ 미착용 3장 연속
                                                   /patrol/hold=True + 부저
                                                              ↓
                                        patrol_node 가 Nav2 목표를 취소하고 정지
                                              (풀리면 그 지점으로 이어서 간다)
소화기 점검 : 웹캠 스트림 정지(/webcam/enable=False) → CSI 를 그 순간만 띄워 촬영
```

두 스트림을 동시에 켜지 않는 이유는 성능이 아니라 **무선**이다(아래 "지금 남은 문제").
전환은 helmet_node 가 `/inspect/start`(내림)와 `/inspect/status` 의 `done`(올림)을
듣고 `/webcam/enable` 로 알린다. 순번 자체는 이미 patrol_scheduler 가 배정한다.

#### 만든 것

| 파일 | 위치 | 역할 |
|---|---|---|
| `tools/webcam_node.py` | **로봇** `~/launch/webcam_node.py` | `/dev/video2` → `/webcam/image_raw/compressed` |
| `patrol_core/helmet_node.py` | VM | 사람 검출 + 안전모 판정 + `/patrol/hold` + 부저 + CSV·사진 |
| `tools/helmet_calib.py` | VM | **실제 안전모를 찍어** 색 기준을 `maps/helmet_calib.yaml` 에 등록 |
| `patrol_node` 의 `/patrol/hold` | VM | 주행 중 정지·재개 |

**`cmd_vel_mux` 는 결국 필요 없었다.** 세우는 방법이 Nav2 **목표 취소**이기 때문이다 —
Nav2 가 취소를 받으면 컨트롤러가 스스로 멈춘다. `/cmd_vel` 을 직접 내지 않으므로
절대 규칙 1 을 건드리지 않고, 검증이 끝난 주행 스택도 손대지 않는다.

#### 판정은 안전모 색이 아니라 **머리카락**을 본다 (2026-08-02 실측으로 결정)

사람 상자 **맨 위 6% 띠**에 어두운 화소(V≤115)가 있으면 미착용, 없으면 뭔가 씌워진
것이니 착용이다. 사용자 제안("머리 위에 뭐가 씌어져 있으면 안전모로 봐도 된다")대로
안전모를 맞히지 않고 맨머리를 찾는다.

**왜 색을 버렸나.** 같은 구도에서 착용/미착용을 둘 다 찍어 재보니 흰 안전모는
흰·크림 천장과 색으로 구분되지 않았다:
```
안전모 색 덩어리   착용 0.037~0.069 / 미착용 0.030~0.048   → 안 갈린다
  (덩어리 높이가 양쪽 다 144~155px 로 같았다 = 잡힌 게 안전모가 아니라 천장이었다)
```
얼굴을 찾아 그 위를 보려는 시도도 실패했다 — 바닥에서 올려다보는 각도에서 Haar 는
6장 중 1장만 맞고, 맞을 때도 얼굴 전체가 아니라 **눈 주변 55px**만 잡아(실제 얼굴
약 200px) 환산하면 챙·이마를 보게 된다. 반면 **사람 상자의 위쪽 변은 정확**했다
(상자 top y=114 / 실제 안전모 top y≈112). 그래서 상자 top 을 기준으로 삼는다.

**띠는 얇아야 하고(6%), 덩어리 높이 조건이 반드시 필요하다.**
띠는 상자 폭 전체(약 460px)인데 머리는 115px 뿐이라 남은 75%가 천장이다. 그래서
**천장 타일 이음선** 한 줄(길고 두께 2~4px)이 머리카락과 같은 크기의 어두운 화소를
만들어, 단순 비율로는 안전모를 쓴 사람을 미착용으로 판정했다(실주행에서 실제로 정지가
걸렸다). 머리카락은 띠 높이를 꽉 채우므로 **덩어리 높이 ≥ 띠의 65%** 조건을 걸면
이음선이 걸러진다:

| 조건 | 착용 | 미착용 |
|---|---|---|
| 단순 비율 | 0.000~0.108 | 0.056~0.115 (겹침) |
| **높이 65% 조건** | **0.000 (18장 전부)** | 0.064~0.094 (18/22 검출) |

임계값 `0.02`, 여유 3배 이상. **오탐(안전모 썼는데 세우는 것)은 0**, 놓치는 쪽만
18% 남는데 고립돼 있어 `alert_frames`(3장 연속) 조건에 걸리지 않는다.

**⚠️ 이 방식의 한계** — "머리카락이 보이는가"를 보므로
어두운 색 안전모는 미착용으로, 머리가 밝은 사람(백발·삭발·탈색)은 착용으로 오판한다.
안전모 색이 배경과 뚜렷이 다르면 `method:=color` 가 낫다(아래 색 등록 참고).
머리가 화면 위로 잘리면(`min_top_margin_px`) 판정을 보류한다 — 안 보이는 것은
판정하지 않는다.

#### 색 방식(`method:=color`)의 기준은 찍어서 등록한다

`gauge_calib.yaml` 과 같은 방식이다. 안전모를 바꾸거나 조명이 달라져도 **코드는 그대로**,
기준만 다시 잡는다:
```
python3 ~/vibe/ex1/tools/helmet_calib.py --grab --name 노란안전모 --select
python3 ~/vibe/ex1/tools/helmet_calib.py --list
```
찍은 영역의 HSV 분포에서 범위를 만들고(양끝 10% 버림 + 여유), 그 범위가 선택 영역을
몇 % 덮는지·사진 전체의 몇 %가 같은 색인지를 같이 알려준다. 전체 비율이 높으면
배경을 안전모로 오인한다는 뜻이라 그 자리에서 다시 잡을 수 있다.
`maps/helmet_calib.yaml` 이 없으면 내장 일반값(흰/노랑/파랑/빨강)으로 돌아간다.

#### 탐지 방식

| 방법 | 설치 | 상태 |
|---|---|---|
| `cv2.dnn` + MobileNet-SSD | `models/` 에 파일 2개 (**받아 뒀다**, 23MB) | **지금 이걸로 돈다.** `detector:=auto` 가 자동으로 고른다 |
| HOG (opencv 내장) | 없음 | 모델을 지우면 여기로 돌아간다. 정면 전신에만 맞고 정확도가 낮다 |
| YOLO | pip + 2GB | pip 설치에 sudo 필요 — 택하지 않았다 |

`models/MobileNetSSD_deploy.{prototxt,caffemodel}` 는 chuanqi305/MobileNet-SSD 에서
받았다(sudo·pip 불필요). 모델이 없어도 노드가 HOG 로 돌아가게 해 둔 이유는, 모델을
받기 전에 나머지(정지·부저·기록)를 먼저 검증할 수 있어야 했기 때문이다.
⚠️ **모델의 검출 정확도는 아직 실물로 확인하지 못했다** — 합성 이미지로는 의미가
없어서(사람처럼 안 생겼으니 안 잡힌다) 웹캠이 붙은 뒤 실제 영상으로 봐야 한다.

#### ⚠️ 실주행 전에 확인할 것

1. **웹캠은 라이다 스캔 평면 위로 올려 달 것** (함정 9). 예전에 라이다가 자기 카메라를
   9cm 짜리 가짜 벽으로 봤다. 위로 25~30° 기울여 사람을 본다.
2. **USB 전류.** 라이다가 죽었을 때 웹캠이 꽂혀 있었고 빼고 나서 살아났다. 인과는
   미확정이지만, 꽂은 뒤 `/scan` 이 계속 나오는지 반드시 확인할 것.
3. **USB 를 꽂으면 CSI 인덱스가 0 → 1 로 밀린다.** 점검용 `camera:=1` 은 웹캠을 꽂은
   상태 기준이다. 웹캠을 빼면 다시 0 이다.
4. `camera_ros` 로는 USB 웹캠이 안 뜬다(RGB888 미지원, exit -6). 로봇에 `v4l2_camera`·
   `usb_cam` 도 없다 — 그래서 `webcam_node.py` 를 따로 만들었다(설치 불필요).

#### 카메라 각도를 맞출 때

`live_view.py` 를 웹캠 토픽으로 돌리면 화면을 보면서 기울기를 잡을 수 있다
(게이지용 표시는 무시하고 삼분할 격자만 보면 된다):
```bash
python3 ~/vibe/ex1/tools/live_view.py --topic /webcam/image_raw/compressed
```
판정 기준을 맞출 때는 `helmet_node` 를 세우지 않는 모드로 띄우고 저장된 사진을 본다:
```bash
ros2 run patrol_core helmet_node --ros-args -p hold:=false -p save_all:=true
```
`logs/helmet_<시각>/` 에 사람 상자(초록/빨강)와 머리 영역(하늘색), 안전모 비율 `r=` 이
그려진 사진이, `logs/helmet_<시각>.csv` 에 그 수치가 남는다. `r` 값을 보고
`helmet_ratio` 를 정하면 된다.

#### 검증 결과

**실물 검증 완료 (2026-08-02, 로봇 연결 상태)**

| 항목 | 결과 |
|---|---|
| 웹캠 스트림 | 2.8fps, 34KB/s (요청 3fps) |
| **`/scan` 영향** | **없음** — 웹캠 켜기 전 4.98Hz → 켠 뒤 4.98Hz |
| 사람 검출(MobileNet-SSD) | 확신도 0.86~0.99, 안정적 |
| 판정 정확도 | 착용 18장 **오탐 0** / 미착용 22장 중 18장 검출 |
| 끝까지 동작 | 착용(조용) → 미착용 **정지+부저** → 해제 → 미착용 → 해제, **2사이클** |
| 부저 | `success=True, 'Succeeded to write data'` |

실주행 흐름(19:02:41 시작): 0~31초 착용 조용 → 32초 미착용 정지+부저(0.094) →
45초 해제 → 52초 미착용 정지(0.126) → 61초 해제. 착용 중 잘못 세운 적 없음.

**남은 미검증** — 순찰 주행과 **동시에** 돌린 상태(움직이는 로봇에서의 판정·무선 부하),
점검 순번과의 실제 교대(코드·신호는 검증됨), 다른 사람·다른 조명.

#### 로봇 없이 검증한 것

`/patrol/hold` 로 주행 중 정지(가짜 Nav2 `travel_sec:=6`), 정지 동안 새 목표 없음,
해제 후 **같은 지점으로 이어서** 감, 회차가 끝나버리지 않음. 점검 상호배제
(`/inspect/start` → `/webcam/enable=False` → `done` → `True`).

---

### ② 점검 지점까지 자율 이동 + 정합 0.9 이상 (남은 검증)

실주행에서 정상 판정까지 갔지만, **정합 0.9 이상**은 아직 못 봤다(마지막 회차 0.69,
변화량 0.53으로 정상 판정). 자세를 더 정밀하게 맞추면 올라간다 — 지금은 카메라 정렬
허용오차가 120px 이라 그만큼 여유가 남는다. 급하지 않다.

### ③ QR 내용연한

`tools/make_qr.py` 로 만들고 `zbarimg` 로 검증까지 끝났다(`logs/qr.pdf` = 100mm 2장).
남은 것: **라벨의 내용연한 연도를 읽어** QR 내용을 확정하고(`FE-01|2030-11` 형식,
버전1 한계 14바이트), 5cm 크기로 인쇄해 **평면에**(곡면 금지) 게이지 근처 높이에 붙이고,
38cm 거리에서 판독되는지 확인. 판정은 정상/임박(3개월)/만료/판독불가 4단계.
만료는 계속 유지되는 상태라 **최초 발견 시만 부저**, 이후는 기록만 할 계획.

### ④ (①로 옮김) 안전모·사람 감지 — 완료

### 그 다음
- **4단계** 소화기 좌표 · 금지구역 다각형 등록 (`fire_extinguisher_points.yaml`, `restricted_zones.yaml`). 자동 탐지 아님, 수동 등록
- **6단계** 야간 순찰 / 금지구역 침입 경보 — (금지 시간대) AND (로봇이 금지구역 다각형 안) AND (YOLO 사람 탐지) AND (바운딩박스 높이 ≥ 실측 기준). 기존 노드에 조건문 추가
- 카메라 정면 고정 상태에서 안전모 탐지 · 소화기 QR

### 개선점
- ~~`patrol_node` 가 목표 거절 시 에러만 반복 출력한다~~ → 원인 후보를 안내하고 5초로 조였다
- 스케줄러가 야간·주간을 구분하지 않는다. 6단계에서 금지 시간대를 다룰 때 함께 볼 것
- ~~`tools/grab_shot.py` scp 호출 버그~~ → ssh 한 번으로 stdout=tar / stderr=로그 로 바꿔 해결. 취득 코드는 `patrol_core/shot_grab.py` 로 옮겨 `inspect_node` 와 공유한다
- `tools/gauge_calib.py` 자동 ROI 가 라벨 로고를 잡는다 → 위쪽 30% 로 제한하거나 경고 추가
- ~~`patrol_scheduler` 의 점검 배정 미검증~~ → 순찰↔점검 순번 교대 검증 완료
- 점검 1지점에 27~43초가 든다(대부분 카메라 기동). 지점이 늘면 `manage_camera:=false` 로 한 번만 켜는 편이 빠를 수 있다 — 대신 주행 중 무선 포화를 감수해야 한다

---

## 지금 남은 문제 — 무선 지연 (2026-08-01 미해결)

**증상.** Nav2 로그에 이게 계속 찍힌다:

```
amcl / local_costmap / global_costmap:
  Message Filter dropping message: frame 'base_scan' ...
  'the timestamp on the message is earlier than all the data in the transform cache'
DWBLocalPlanner: No valid trajectories out of 819!
controller_server: Failed to make progress  ->  목표가 status=6 으로 즉시 중단
```

**원인.** `/scan` 이 늦게 도착해 TF 캐시 시간을 벗어나 **통째로 버려진다.** 그러면
AMCL 이 위치추정을 못 하고(오늘 소화기에서 1m 빗나간 원인), 코스트맵이 비어 경로를
못 만든다. 순찰이 0.1분 만에 "완주"로 끝나는 것도 이것 때문이다.

**측정값.** ping 이 5ms ~ 1500ms 사이를 오간다. 로봇→공유기 29ms 인데 **VM→공유기가
200ms** 다. 로봇 무선 신호는 −29dBm 으로 강하다 → **호스트 PC 쪽 무선 구간이 병목**이다.

**해결 순서:**
1. **호스트 PC 를 유선 랜에** 꽂는다 (근본. 오늘 문제 대부분이 여기서 나온다 —
   사진 취득 실패, 목표 거절, 위치추정 불량, 스캔 폐기)
2. 안 되면 Nav2 시간 허용치를 늘린다(완화):
   `config/patrol_nav2.yaml` 의 `transform_tolerance` costmap 0.2 → 1.0, AMCL 1.0 → 2.0.
   늦게 온 스캔도 받아들인다. 정밀도는 떨어지지만 통째로 버리는 것보단 낫다
3. 순찰·주행 중에는 카메라 스트림을 켜지 않는다 (이미 코드가 지킨다)

## 프로젝트 절대 규칙

1. **`/cmd_vel` 은 `cmd_vel_mux` 만 발행한다.** 다른 노드는 `/cmd_vel_nav`, `/cmd_vel_teleop` 등으로 낸다
2. 라인 추종과 Nav2 를 동시에 켜지 않는다 (라인 추종은 폐기됨)
3. 영상 인식 연산(YOLO 등)은 로봇이 아닌 **VM** 에서 수행
4. 소화기 QR 점검(인식 노드 + 왕복)은 이번 범위에서 제외
